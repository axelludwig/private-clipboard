CREATE TABLE `Clips` (
	`id` INT NOT NULL AUTO_INCREMENT,
	`content` TEXT,
	`imagesrc` varchar(255),
	`time` DATETIME NOT NULL,
	`private` BOOLEAN NOT NULL,
	PRIMARY KEY (`id`)
);

insert into
	Clips (content, time, private)
values
	('test', CURRENT_TIMESTAMP, false);

	